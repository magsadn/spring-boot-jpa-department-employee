spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/department-employee
    username: postgres
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: true
server:
  port: 8088
  servlet:
    context-path:
